{
  "name": "f1page",
  "version": "1.0.0",
  "private": true,
  "description": "The championship order as a descent — scroll distance between teams is the points gap, to scale.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "serve": "node serve.js",
    "dev": "node build.js && node serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
