{
  "name": "downforce",
  "version": "1.0.0",
  "private": true,
  "description": "The F1 championship as a descent — scroll distance between teams is the points gap, to scale.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "serve": "node serve.js",
    "dev": "node build.js && node serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
